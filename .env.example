PORT=8080
SERVICETITAN_ENV=integration
SERVICETITAN_CAMPAIGN_ID=your-campaign-id
# Optional: used when match-technicians returns no rows (last-resort for Retell flows)
# SERVICETITAN_FALLBACK_TECHNICIAN_ID=14721
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

================================================
# buildops creds
BUILDOPS_CLIENT_ID = 
BUILDOPS_CLIENT_SECRET = 
BUILDOPS_TENANT_ID =  

=================================================
RETELL_API_KEY = "your-retell-api-key"
BASE_URL = "https://api.retellai.com"
LLM_ID = "your-llm-id"

#using builodps client id and client secret we get access token 
# using which we make api calls
